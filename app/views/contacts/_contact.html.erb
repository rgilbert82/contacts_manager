<div class="contact_container">
  <div class="well span8">
    <h4><strong><%= link_to contact.name, contact_path(contact) %><strong></h4>
    <h5><strong>Email: </strong><a href="mailto:<%= contact.email %>"><%= contact.email %></a></h5>
    <h5><strong>Phone: </strong><%= format_phone_number(contact.phone) %></h5>
  </div>
</div>
